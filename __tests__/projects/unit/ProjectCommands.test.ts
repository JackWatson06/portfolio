
test('we can create a new project.', () => {

});

test('we get correct message on duplicate project.', () => {

});

test('we get correct message on invalid project.', () => {

});

test('we can only create a project as an admin.', () => {

});























test('we can fetch single project.', () => {

});

test('we get 404 on non existent project.', () => {

});

test('we can only fetch a single project as an admin.', () => {

});

test('we can fetch single public project.', () => {

});

test('we get 404 on non existent public project.', () => {

});

test('we can fetch all public projects.', () => {

});

test('we can fetch all projects.', () => {

});

test('we can only fetch private projects as an admin.', () => {

});

test('we can update a project.', () => {

});

test('we get 404 on project update that does not exist.', () => {

});

test('we get 400 on invalid update to a project.', () => {

});

test('we get 400 on duplicate update to a project.', () => {

});

test('we can delete a project.', () => {

});

test('we get 404 on project delete that does not exist.', () => {

});

test('we can only update project as .', () => {

});

